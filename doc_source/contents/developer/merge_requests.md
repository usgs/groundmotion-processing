# Release Steps

1. Update the changelog (`doc_source/contents/developer/changelog.md`) for significant changes
2. Update docs if relevant
3. Add unit tests for any new features
4. Run the unit tests (we use ``pytest``) prior to sending in your changes.
